schema_version: 1
suite_id: smoke
operators:
  include: ["example_cpu_add", "deepseek_v4_fp8_gemm_nt"]
  exclude: []
candidates:
  include: ["quickstart__*", "pytorch_dequant__*"]
cases:
  tags: [smoke]
mode: correctness
correctness:
  seeds: [0]
performance:
  samples: 3
  inner_iterations: 1
