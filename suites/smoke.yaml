schema_version: 1
suite_id: smoke
operators:
  include: ["example_cpu_add"]
  exclude: []
candidates:
  include: ["quickstart__*"]
cases:
  tags: [smoke]
mode: correctness
correctness:
  seeds: [0]
performance:
  samples: 3
  inner_iterations: 1
